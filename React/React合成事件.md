## 磨平浏览器差异
- 将原生事件统一封装成`SyntheticEvent`对象，无论在Chrome还是Safari上点击，拿到的`event`对象的属性和方法都是一致的
- 手动实现了在不同浏览器里表现不一的事件，确保逻辑在所有环境下的一致性

## 性能优化
事件委托，不会把事件挂载到每个具体的元素（button或div）上
只需要一个监听器，就能处理整个应用的所有同类事件，当组件销毁时会自动清理映射关系，减少内存泄漏的风险和内存占用

## 统一调度
通过合成事件可以在第一时间打上优先级标签：onClick产生的更新优先级高，onScroll产生的更新优先级较低
